# CLion specific IDE config files
.idea

# Make results
iso
iso-test
iso-keytest
iso-rawtest
iso-editortest
iso-reboottest
*.iso
*.o
*.elf

# Checkpoint U: generated on demand via `make disk` -- not committed, same
# reasoning as the FAT32 test image below.
/disk-build
/disk-fat.img
/zephyrlite-i386.img

# VM files
*.qcow2

# Rust build artifacts
/kernel/target
userland/drivers/console_server/target
userland/cap_test/target
userland/services/nameservice/target
userland/drivers/storage_ata/target
userland/services/fs_fat32/target
userland/libpcern/target
userland/bin/shell/target

# Assembled userland test binaries
userland/*.bin

# Checkpoint K: generated on demand via `make test-fat32-image` from
# testdata/ -- not committed (see Makefile).
/test_fat32.img
