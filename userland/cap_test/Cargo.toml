[package]
name = "cap_test"
version = "0.1.0"
edition = "2021"

[dependencies]
libpcern = { path = "../libpcern" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
