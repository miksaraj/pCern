[package]
name = "console_server"
version = "0.3.0"
edition = "2021"

[[bin]]
name = "console_server"
path = "src/main.rs"

[dependencies]
libpcern = { path = "../../libpcern" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
