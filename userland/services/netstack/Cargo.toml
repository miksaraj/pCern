[package]
name = "netstack"
version = "0.2.0"
edition = "2021"

[[bin]]
name = "netstack"
path = "src/main.rs"

[dependencies]
libpcern = { path = "../../libpcern" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
